node_modules/
dist/
.wrangler/
*.local

# Secrets — never commit
.dev.vars
.env
*.pem
service-account*.json

# Editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# Logs
npm-debug.log*
*.log

# Design canvas: the .dc.html artboards are the source and are committed; the
# seeded page is a ~2.4 MB build artifact with an editor bundled into it.
design/*.html
!design/*.dc.html
